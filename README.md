# Free Fire JWT Bot

Ye bot Free Fire accounts ke liye JWT generate karta hai.

## Run locally
1. Install dependencies: pip install -r requirements.txt
2. Run: python sendmejwt.py

## Deploy on Vercel
- Set environment variables TOKEN aur API_KEY
- Use webhook deployment
